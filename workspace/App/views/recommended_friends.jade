// Jade file to specify output of an actors list
// Inherits from layout
extend layout
block content
  p
    | #{title}:
  // Create a table with one row per result
  table
    - for (var inx = 0; inx < friends.length; inx++)
        - if(inx >= 10) break;
        tr
          td
            a(href="/user/" + friends[inx].U_ID)
              span=friends[inx].FIRST_NAME+" "+friends[inx].LAST_NAME+" "
        tr
          td.PostSeparatorOuter
        tr
          td.PostSeparatorInner
        tr
          td.PostSeparatorOuter
                 
                 
